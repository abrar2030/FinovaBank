# Backend Configuration Example for Terraform State Management
# 
# REMOTE BACKEND (Production - Recommended)
# Copy this file to backend.tf and update with your S3 bucket details
# Uncomment and configure one of the backend options below

# Option 1: S3 Backend (Recommended for production)
# Requires: S3 bucket and DynamoDB table created beforehand
# 
# terraform {
#   backend "s3" {
#     bucket         = "YOUR-TERRAFORM-STATE-BUCKET"
#     key            = "infrastructure/terraform.tfstate"
#     region         = "us-west-2"
#     encrypt        = true
#     dynamodb_table = "YOUR-TERRAFORM-LOCK-TABLE"
#     kms_key_id     = "arn:aws:kms:REGION:ACCOUNT:key/KEY-ID"  # Optional but recommended
#   }
# }

# Steps to set up S3 backend:
# 1. Create S3 bucket: aws s3 mb s3://your-terraform-state-bucket --region us-west-2
# 2. Enable versioning: aws s3api put-bucket-versioning --bucket your-terraform-state-bucket --versioning-configuration Status=Enabled
# 3. Create DynamoDB table: aws dynamodb create-table --table-name your-terraform-lock-table --attribute-definitions AttributeName=LockID,AttributeType=S --key-schema AttributeName=LockID,KeyType=HASH --billing-mode PAY_PER_REQUEST
# 4. Copy this file to backend.tf and update the values
# 5. Run: terraform init -migrate-state

# Option 2: Local Backend (Development/Testing only)
# No configuration needed - Terraform uses local backend by default
# State file will be stored in: ./terraform.tfstate
# 
# WARNING: Local backend is NOT recommended for:
# - Production environments
# - Team collaboration
# - CI/CD pipelines
# 
# For local development, you can simply delete the backend block from main.tf
