---

# templates/service-savings-goals.yaml
apiVersion: v1
kind: Service
metadata:
  name: savings-goals-service
  labels:
    app: savings-goals-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8094
      targetPort: 8094
  selector:
    app: savings-goals-service